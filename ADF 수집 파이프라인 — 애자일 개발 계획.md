
# ADF 수집 파이프라인 — 애자일 개발 계획
 
> **문서 목적**: 설계·개발을 반복(iterative) 방식으로 진행하기 위한 계획. **어디에 애자일을 적용하고 어디에 적용하지 않을지**를 명확히 하는 것이 이 문서의 핵심이다.
 
| 항목 | 내용 |
|---|---|
| 관련 문서 | ADF 메타 테이블 · 파이프라인 설계 오버뷰 / DDL 변경 체크리스트 |
| 작성 | 2026-07-28 / v1.0 |
 
---
 
## 1. 적용 배경
 
### 영역별 요건 명확도가 크게 다르다
 
| 영역 | 요건 명확도 | 변경 가능성 | 근거 |
|---|---|---|---|
| 초기 / FULL 수집 | **높음** | 낮음 | 추출 조건이 `1=1` 로 단순. 확인할 변수가 적다 |
| 증분 — WINDOW | 중 | 중 | 주기·겹침 폭이 실데이터로 검증되어야 확정된다 |
| 증분 — BIZDAY·CLOSE | **낮음** | **높음** | 조건값 산출 방식(표현식/소스 조회)이 미확정 |
| 백필 · adhoc | **낮음** | **높음** | 실제 운영 요청이 나와야 형태가 잡힌다 |
| 로깅 · 모니터링 · 대시보드 | **가장 낮음** | **가장 높음** | 써보기 전에는 무엇이 필요한지 알 수 없다 |
 
**한 방식으로 전체를 진행하면 어느 한쪽이 손해**를 본다. 명확한 영역까지 반복하면 낭비이고, 불확실한 영역을 한 번에 확정하려 하면 재작업이 발생한다.
 
### 반복이 가능한 조건이 갖춰져 있다
 
| 조건 | 상태 |
|---|---|
| 운영계 초기 이행 | **별도 방식으로 진행 중** — 이 설계가 운영 일정의 크리티컬 패스가 아니다 |
| 테스트계 메타 제약 변경 | **자유롭게 가능** |
| 소스 접근 · 계정 · 방화벽 | **해결 완료** |
 
운영 압박 없이 테스트계에서 반복할 수 있는 상태다. **이 조건이 애자일 적용의 전제**이며, 조건이 바뀌면 계획도 재검토해야 한다.
 
---
 
## 2. 적용 원칙
 
### 바꾸기 어려운 것은 일찍, 쉬운 것은 늦게
 
| 영역 | 변경 비용 | 방식 | 확정 시점 |
|---|---|---|---|
| 메타 모델 · 제약 · 락 체계 | **높음** | 선확정 → S4까지 조정 허용 | **S3 종료** |
| 초기 / FULL 수집 | 중 | 계획형, 1회 완성 | S1 |
| 증분 (WINDOW) | 중 | 반복 | S2 |
| 증분 (BIZDAY·CLOSE·TEMPLATE) | 중 | 반복 | S3 |
| 백필 · adhoc | 낮음 | 반복 | S4~S5 |
| 로깅 · 모니터링 · 대시보드 | 낮음 | **반복 필수** | 지속 |
 
> **"애자일이니까 전부 나중에"가 아니다.** 되돌리기 비싼 결정(DDL·제약·락 체계)을 먼저 고정해야 나머지를 자유롭게 반복할 수 있다. 메타 모델은 이미 확정되어 있고, S4부터는 프리즈한다.
 
### DDL 프리즈를 S4로 두는 이유
 
S4에서 대시보드와 ES 인덱스 매핑이 컬럼 구조에 의존하기 시작한다. 그 시점 이후의 스키마 변경은 **파이프라인 + 대시보드 + 로그 매핑 세 곳**에 파급된다.
 
| 구간 | DDL 취급 |
|---|---|
| **S1 ~ S3** | 변경 자유. 테스트계 **DROP + CREATE 재생성**, 마이그레이션 스크립트 미작성 |
| **S4** | 프리즈 시작. 변경 시 영향 분석 필수 (→ DDL 변경 체크리스트) |
| **S5 이후** | 동결. 마이그레이션 + 롤백 스크립트 세트로만 변경 |
 
---
 
## 3. 스프린트 계획
 
| # | 범위 | 데모 · 검증 | DDL |
|---|---|---|---|
| **S1** | 메타 DDL · 공통 파이프라인 3종 · 초기/FULL 수집 · 차일드 1개 DB | 테스트계 소량 적재 (`P_ROW_LIMIT`) — 이력 기록·상태 집계 정상 | 자유 |
| **S2** | 증분 WINDOW · 만회 모드 · 디스패처 | **의도적 실패 주입 → 다음 회차 자동 회수 시연** | 자유 |
| **S3** | BIZDAY·CLOSE 조건값 조회 · TEMPLATE · 조건값 공유 | 실제 영업일·마감 로직으로 조건값 검증 | 자유 |
| **S4** | 로깅(ES 연계) · **대시보드 v1** · 백필 | **운영자에게 화면 시연** — 판단 가능한가 | ⚠ 프리즈 |
| **S5** | adhoc · 운영 도구(락 해제·대상 등록) · Runbook · **운영 전환 리허설** | 대상 5~10건 병행 운영 | 동결 |
 
### 스프린트별 핵심 검증 포인트
 
**S1 — 골격이 서는가**
공통 파이프라인 3종(`pl_set_master_pl`·`pl_dispatch_ingest`·`pl_call_dbx_api`)과 이력 구조가 의도대로 동작하는지 확인한다. 수집 자체보다 **이력이 정확히 남는지**가 관건이다.
 
**S2 — 설계의 핵심 주장을 검증한다**
정상 수집 성공은 최소 조건이고, **실패 후 다음 회차가 놓친 구간을 회수하는지**가 진짜 검증이다. 이 설계가 "미수집을 원인 구분 없이 자동 회수한다"고 주장하므로, 그 주장을 실증하지 못하면 나머지 논의가 무의미하다.
 
**S3 — 요건 불확실성이 가장 큰 구간**
CLOSE의 산출 방식(표현식 vs 소스 조회)이 여기서 확정된다. 설계상 양쪽을 모두 수용하므로 **어느 쪽으로 밝혀져도 메타 등록값만 달라진다.**
 
**S4 — 운영 이관 가능성이 판가름난다**
대시보드가 없으면 운영자는 ADF 화면과 메타 DB 양쪽을 봐야 한다. 이 상태로는 이관이 어렵다. **화면 하나로 아침 점검이 되는지**를 운영자에게 직접 확인받는다.
 
**S5 — 실제로 넘길 수 있는가**
기능이 아니라 절차를 검증한다. Runbook을 보고 운영자가 장애 대응을 할 수 있는지, 병행 운영이 안전한지를 확인한다.
 
---
 
## 4. 완료의 정의 (DoD)
 
**"돌아간다"는 완료가 아니다.** 각 수집 유형은 아래 5개를 모두 만족해야 완료로 본다.
 
| # | 조건 | 확인 방법 |
|---|---|---|
| 1 | 정상 수집 성공 | 이력 `SUCCEEDED`, 건수 일치 |
| 2 | 실패 시 이력에 `FAILED` + `error_log` 기록 | 소스 차단 등으로 실패 주입 |
| 3 | **실패 후 다음 회차가 놓친 구간을 회수** | 회차 2회 실행, 두 번째 회차의 하한값 확인 |
| 4 | 진행점(HWM·`last_success_dt`)이 **성공 시에만** 전진 | 실패 회차 전후 값 비교 |
| 5 | 재시도 시 **같은 구간**을 읽음 (멱등성) | 이력의 `extract_query` 동일성 확인 |
 
**3·5번을 DoD에 넣지 않으면 설계의 핵심 장치가 운영에 들어가서야 검증된다.** 개발 단계에서 실패를 일부러 만들어보는 것이 이 프로젝트에서는 필수 절차다.
 
### 조용한 실패 검증
 
기능 검증과 별개로, 아래는 **성공으로 끝나면서 틀리는** 경로다. 각 스프린트에서 해당 항목을 함께 확인한다.
 
| 스프린트 | 확인 |
|---|---|
| S1 | `P_ROW_LIMIT` 미적용 시 전체가 읽히는가 (제한이 남아 있지 않은가) |
| S2 | 하한이 빠진 채 전체 스캔되는 경로가 없는가 |
| S3 | 조건값 0행·플레이스홀더 잔존 시 `FAILED` 로 떨어지는가 |
| S4 | 마스터 `SUCCEEDED` 인데 대상 일부가 실패한 상황이 대시보드에 드러나는가 |
| S5 | 락이 걸린 채 남은 회차가 탐지되는가 |
 
---
 
## 5. 검증 데이터 전략
 
### 행 수 제한은 파라미터로
 
테스트계에서 전체 흐름을 소량으로 검증하기 위해 `P_ROW_LIMIT` 파이프라인 파라미터를 사용한다 (기본 NULL = 제한 없음).
 
| 방식 | 평가 |
|---|---|
| **`P_ROW_LIMIT` 파라미터** | **채택** — 운영 트리거에 넣지 않으면 자연히 미적용 |
| 메타 컬럼 (`test_row_limit`) | 미채택 — **지워야 할 값이 데이터에 남는다** |
| 쿼리 하드코딩 | **금지** — 제거 누락 시 테이블당 N건만 실리고 성공으로 끝난다 |
 
⚠ MSSQL·COSMOS는 `TOP n` 이 SELECT 절에 와야 해 TEMPLATE 쿼리에 기계적 부착이 어렵다. `{ROW_LIMIT}` 플레이스홀더 방식을 S1에서 검토한다.
 
### 검증 대상 선정
 
| 스프린트 | 대상 구성 |
|---|---|
| S1 | 소형 테이블 3~5건, 소스 DB 1개 |
| S2 | + 증분 대상 3~5건 (`incr_column_type` 을 DATE·TIMESTAMP·STRING 으로 분산) |
| S3 | + BIZDAY·CLOSE·TEMPLATE 각 1건 이상 |
| S4~S5 | + 소스 DB 2개 이상, 대용량 1건 (SHIR 부하 측정) |
 
`incr_column_type` 을 분산하는 이유는 **타입별 캐스팅·포맷 처리가 갈리기 때문**이다. 특히 `NUMERIC` 은 문자열 비교 시 `'9' > '10'` 이 되므로 별도 확인이 필요하다.
 
---
 
## 6. 대시보드 반복 계획
 
대시보드는 **써보기 전에는 무엇이 필요한지 알 수 없다.** 처음부터 완성하려 하면 안 쓰는 화면만 남는다.
 
| 버전 | 내용 | 피드백에서 확인할 것 |
|---|---|---|
| **v1** | **지연 대상 1화면** (`last_success_dt` 기준) | 이 화면만으로 아침 점검이 되는가 |
| **v2** | 회차 성공률 · 스킵률 · 실패 대상 + 원인 | 어떤 단위로 보고 싶은가 (파이프라인별/DB별/대상별) |
| **v3** | 건수·용량·소요시간 추이, End-to-End 대조 | 이상 탐지에 실제로 쓰이는가 |
 
**v1은 쿼리 하나짜리다.** 스프린트를 다 쓰지 않으며, 일찍 내놓고 운영자 반응을 받는 것이 목적이다.
 
### 지표는 하나에서 시작한다
 
> **"며칠째 성공 없는 대상이 있는가?"**
 
`last_success_dt` 하나로 차일드 실패·트리거 스킵·트리거 미발화·조건 튜플 오등록이 전부 걸린다. **원인별로 다른 화면을 만들 필요가 없다.**
 
```sql
SELECT src_db_nm, src_table_nm, condition_frequency, last_success_dt,
       now() - last_success_dt AS 지연
FROM   ctl_ingest_target_master
WHERE  delete_yn = 'N' AND target_kind = 'TABLE'
AND    last_success_dt < now() - interval '2 days'
ORDER  BY last_success_dt;
```
 
---
 
## 7. 로그 · 모니터링 아키텍처
 
⚠ *고객사 Elasticsearch 구성·표준 수집 경로를 확인하지 못해 아래는 전제 기반이다.*
 
### 원천을 분리한다
 
| 구분 | 저장소 | 역할 |
|---|---|---|
| **진실의 원천 (SoR)** | 메타 DB (PostgreSQL) | 제약 · FK · **락** · 트랜잭션. 파이프라인이 실제로 읽고 쓰는 곳 |
| **조회 · 시각화** | Elasticsearch | 검색 · 집계 · 대시보드. **읽기 전용 사본** |
 
**메타 이력은 ES로 대체할 수 없다.** 동시수행 락(유니크 인덱스), 대상 선별(제약 기반 조회), 만회 판정이 관계형 트랜잭션에 의존한다.
 
반대로 **로그 검색 · 장기 보관 · 전사 통합** 관점에서는 ES가 적합하며, 고객사 표준이라면 따르는 것이 옳다.
 
### 연계 방식 후보 (S4에서 검증)
 
| 방식 | 특징 |
|---|---|
| 메타 DB → ES 주기 동기화 | 단순. 조회 지연 허용 시 무난 |
| ADF에서 이중 기록 | 즉시성은 좋으나 **한쪽 실패 시 정합성이 깨진다** — 비권장 |
| 수집기(Logstash 등)로 폴링 | 고객사 표준 파이프라인이 있다면 자연스럽다 |
 
### 확인 필요 사항
 
| # | 확인 |
|---|---|
| 1 | 고객사 ES에 적재하는 **표준 경로가 이미 존재하는가** |
| 2 | 대시보드를 **Kibana로 만들 것인가, 별도 BI인가** |
| 3 | **메타 DB 직접 조회 기반 대시보드는 허용되지 않는가** |
 
**3번이 리드타임을 좌우한다.** ES를 반드시 거쳐야 한다면 대시보드 v1 시점이 뒤로 밀린다.
 
---
 
## 8. 변경 요청 처리 규칙
 
| 변경 유형 | 처리 | 근거 |
|---|---|---|
| **메타 등록값** (조건·주기·쿼리 템플릿·대상 추가) | **스프린트 중 즉시** | 설계 변경이 아니다 — 이 아키텍처가 원래 흡수하도록 만든 것 |
| 대시보드 | **스프린트 중 반영** | 피드백이 목적이다 |
| 파이프라인 로직 | 다음 스프린트 백로그 | |
| **메타 DDL · 제약 · 락 인덱스** | **영향 분석 후 별도 합의** | → DDL 변경 체크리스트 |
 
> **"메타 등록값 변경은 설계 변경이 아니다"** 를 명시해두면, 조건 튜플이나 쿼리가 바뀌는 것을 변경 요청으로 계상하지 않을 수 있다. 이 아키텍처를 택한 이유 자체가 그런 변화를 개발 없이 흡수하기 위해서다.
 
---
 
## 9. 선행 리스크 · 운영계 전환
 
### 남은 선행 항목
 
| 항목 | 내용 | 착수 시점 |
|---|---|---|
| **SHIR 용량 실측** | 대량 수집을 해봐야 알 수 있다. 부족 시 스케일 요청에 리드타임이 필요 | **S4까지 1회 이상 측정** |
 
기존 선행 리스크 3건(초기 이행 · 메타 제약 변경 비용 · 소스 접근)은 해소되었다.
 
### 운영계 전환 — 병행 운영이 가능하다
 
현재 운영계는 별도 방식으로 초기 이행 중이다. 신 설계로의 전환은 **절체가 아니라 점진 확대**로 진행할 수 있다.
 
| 조건 | 결과 |
|---|---|
| 신 설계는 **메타에 등록된 대상만** 처리 | 일부 대상만 등록해 **점진 전환** 가능 |
| 같은 테이블을 두 방식이 동시에 읽음 | **Bronze dedup이 흡수** — 중복 적재가 무해하다 |
| 전환 시 기존 방식의 진행 지점을 모름 | **넉넉히 과거부터 읽으면 된다** — 중복은 안전, 누락만 위험 |
 
**HWM 초기값을 정확히 맞출 필요가 없다는 것이 전환을 쉽게 만든다.** 기존 방식이 읽은 지점보다 이전으로 잡으면 겹치는 구간은 Bronze가 정리한다.
 
**전환 순서 (S5 리허설 대상)**
 
```
1. 대상 5~10건을 메타에 등록 (init_wait_yn='Y')
2. 신 설계로 초기수집 → 정규 수집 진입
3. 기존 방식과 병행 운영하며 결과 대조
4. 검증되면 기존 방식에서 해당 대상 제외
5. 대상 범위를 단계적으로 확대
```
 
---
 
## 10. 협의 필요 사항
 
| # | 항목 | 대상 |
|---|---|---|
| 1 | 스프린트 주기 (2주) · 데모 참석 범위 | 고객 · 다름테크 |
| 2 | ES 연계 방식 및 대시보드 도구 (7장 확인 3건) | 고객 |
| 3 | 대시보드 v1 피드백 제공 주체 (운영 담당자 지정) | 고객 |
| 4 | 운영계 전환 시점 · 병행 운영 기간 | 고객 |
| 5 | S3 CLOSE 실제 요건 (표현식 vs 소스 조회) | 고객 |
| 6 | 검증 대상 테이블 선정 (스프린트별) | 고객 · 다름테크 |
 
